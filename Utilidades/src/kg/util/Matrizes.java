package kg.util;

public class Matrizes
{
	public static double[][] inverter(double[][] entr)
	{
		if (entr.length != entr[0].length)
			throw new IllegalArgumentException("Matriz não quadrada");
		
		int tam = entr.length;
		double[][] saída = new double[tam][tam];
		
		
		
		return saída;
	}
}
